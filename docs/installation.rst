============
Installation
============

At the command line::

    $ easy_install pg_projector

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pg_projector
    $ pip install pg_projector
